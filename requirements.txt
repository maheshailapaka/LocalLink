flask>=3.0
flask-socketio>=5.3
# Choose ONE async backend for production (comment out the other):
# eventlet>=0.35          # recommended for production
# gevent>=23.0
# gevent-websocket>=0.10
werkzeug>=3.0
